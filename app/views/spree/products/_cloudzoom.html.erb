<%= link_to(image_tag(i.attachment.url(:mini)), i.attachment.url(:product),
            data: {
                cloudzoom:
                    "zoomSizeMode: 'zoom',
                    zoomFlyOut: false,
                    zoomImage: '#{ i.attachment.url(:large) }',
                    animationTime: 100,
                    tintOpacity: 0.2",
                zoomimage: "#{ i.attachment.url(:large) }" })
%>